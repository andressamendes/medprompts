name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Verify build output
        run: |
          echo "📦 Build completed. Verifying output..."
          ls -lh dist/assets/index-*.js || echo "No index bundle found"
          BUNDLE_SIZE=$(stat -c%s dist/assets/index-*.js 2>/dev/null || echo "0")
          echo "Bundle size: $BUNDLE_SIZE bytes"
          if [ "$BUNDLE_SIZE" -lt 300000 ]; then
            echo "⚠️ WARNING: Bundle smaller than expected ($BUNDLE_SIZE bytes)"
          else
            echo "✅ Bundle size OK ($BUNDLE_SIZE bytes)"
          fi
          FOCUMON_COUNT=$(grep -o "focumon" dist/assets/index-*.js 2>/dev/null | wc -l || echo "0")
          echo "Focumon occurrences in bundle: $FOCUMON_COUNT"
          if [ "$FOCUMON_COUNT" -eq "0" ]; then
            echo "⚠️ WARNING: Focumon code not found in bundle"
          else
            echo "✅ Focumon code present in bundle ($FOCUMON_COUNT occurrences)"
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
