# Server Configuration
NODE_ENV=development
PORT=2567

# Frontend CORS
FRONTEND_URL=http://localhost:5173
# Production: https://andressamendes.github.io

# JWT Authentication (MUST MATCH MAIN BACKEND!)
JWT_SECRET=your_jwt_secret_here_change_in_production

# PostgreSQL Database (SHARED WITH MAIN BACKEND)
# Option 1: Individual environment variables (local development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=medprompts
DB_USER=postgres
DB_PASSWORD=your_password_here

# Option 2: Connection string (Render.com / Production)
# DATABASE_URL=postgresql://user:password@host:port/database
# Render automatically provides DATABASE_URL when you create a PostgreSQL service

# Logging
LOG_LEVEL=info
