# Ambiente
NODE_ENV=development

# Servidor
PORT=3001
FRONTEND_URL=http://localhost:5173

# JWT (GERAR CHAVES NOVAS COM: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=TROCAR_POR_CHAVE_SEGURA_128_CARACTERES
JWT_REFRESH_SECRET=TROCAR_POR_CHAVE_SEGURA_128_CARACTERES
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Banco de Dados
DB_HOST=localhost
DB_PORT=5432
DB_NAME=medprompts
DB_USER=postgres
DB_PASSWORD=TROCAR_PELA_SUA_SENHA

# Criptografia (GERAR COM: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=TROCAR_POR_CHAVE_HEX_64_CARACTERES
ENCRYPTION_IV=TROCAR_POR_IV_HEX_32_CARACTERES

# Sessão (GERAR COM: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=TROCAR_POR_CHAVE_SEGURA

# CORS
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE_PATH=logs/

# Backup
BACKUP_ENABLED=false
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30

# AWS S3 (Opcional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
S3_BUCKET_NAME=

# Email (Opcional)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Bcrypt
BCRYPT_ROUNDS=12

# Cache
CACHE_TTL=3600
